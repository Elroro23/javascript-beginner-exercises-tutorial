function standardsMaker() { //Declaramos una función sin parámetros.
    for (let i = 1; i <= 300; i++) { //Definimos un bucle para que inicie en 1, termine en 300 e incremente 1 en cada iteración.
        console.log(`I will write questions if I'm stuck`); //Imprimmos este string.
    }
}

standardsMaker(); //Llamamos a la función.