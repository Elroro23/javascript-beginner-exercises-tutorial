//Declaramos dos variables con un "string" como valor a cada una.
let myVar1 = 'Hello';
let myVar2 = 'World';

//Concatenamos las variables declaradas anteriormente en una nueva.
let theNewString = myVar1+' '+myVar2; //Es recomendable usar "template literals"(`${}) para concatenar.
console.log(theNewString); //Llamamos a la vcariable "theNewString" y la imprimimos.